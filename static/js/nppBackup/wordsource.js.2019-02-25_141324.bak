var available_ws_app = new Vue({
	el: "#available_wordsources",
	data: {
		message: "Hello Vue!"
	}
})

var APP = null;

// This will make everything accessible from the js console;
// for instance, self.x above would be accessible as APP.x
jQuery(function(){APP = app();});